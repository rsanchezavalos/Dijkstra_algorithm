#include<stdio.h>

// funcion para conocer todos los nodos a los que se puede llegar
// funcion para determinar el nodo más cercano
// funcion para almacenar en un set los nodos a los que ya fuiste
// funcion para incrementar el costo con cada decisión
